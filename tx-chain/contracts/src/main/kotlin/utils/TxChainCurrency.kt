package utils

//import com.r3.corda.lib.tokens.contracts.types.TokenType
import net.corda.core.contracts.Amount

//val ChainTokenType = TokenType("ChainToken", 0)

//fun CHAIN_TOKEN(amount: Int): Amount<TokenType> = com.r3.corda.lib.tokens.contracts.utilities.amount(amount, ChainTokenType)
//fun CHAIN_TOKEN(amount: Long): Amount<TokenType> = com.r3.corda.lib.tokens.contracts.utilities.amount(amount, ChainTokenType)
//fun CHAIN_TOKEN(amount: Double): Amount<TokenType> = com.r3.corda.lib.tokens.contracts.utilities.amount(amount, ChainTokenType)
//val Int.CHAIN_TOKEN: Amount<TokenType> get() = CHAIN_TOKEN(this)
//val Long.CHAIN_TOKEN: Amount<TokenType> get() = CHAIN_TOKEN(this)
//val Double.CHAIN_TOKEN: Amount<TokenType> get() = CHAIN_TOKEN(this)
